# 上傳 GitHub Pages：最短步驟

**不要直接把 ZIP 上傳當網站。**

1. 在電腦上解壓縮 `cpc_knowledge_graph_v2_github_pages.zip`
2. 進入解壓縮後的資料夾
3. 將裡面的：
   - `index.html`
   - `11501-09.pdf`
   - `articles` 資料夾
   - `data` 資料夾
   - `README.md`
   全部放到同一個 GitHub Repository 根目錄。
4. 開啟 GitHub → Repository → Settings → Pages
5. 選 `Deploy from a branch`
6. `main` + `/ (root)` → Save

注意：請保留資料夾結構，不要把 articles 裡的 JSON 全部搬到根目錄。
